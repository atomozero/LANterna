# Artefatti di build del core/CLI portabile
*.o
*.d
/lanterna-cli
/LANterna

# Artefatti tipici Haiku (build futuro UI)
/objects.*
/*.x86
/LANterna.rsrc
/xres.output.rsrc

# Catalog binari (.catalog) rigenerati da `make catalogs` dai .catkeys
locales/*.catalog

# hpkg staging + output di `make -f Makefile.haiku hpkg`
/build/
/lanterna-*-x86_64.hpkg
